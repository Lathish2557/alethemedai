import SwiftUI

struct ActivityView: View {
    var body: some View {
        VStack {
            Text("Activity")
                .font(.largeTitle).bold()
            Text("Your recent activity & updates")
        }
    }
}
